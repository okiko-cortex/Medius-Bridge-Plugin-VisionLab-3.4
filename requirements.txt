# Medius firmware 3.4.1 needs the 3.4.1 library (protocol 8, 6 Mbaud control link).
# Pull the matching wheel straight from the GitHub release; PyPI may lag the firmware release.
# Do not replace this with an older PyPI medius package: 3.4.0 speaks protocol 7 and cannot
# talk to a 3.4.1 box.
# pip cannot resolve that from PyPI today.
# Offline fallback: the same wheel ships in this plugin's wheels\ folder (see README, Upgrading).
medius @ https://github.com/K4HVH/medius/releases/download/v3.4.1/medius-3.4.1-py3-none-win_amd64.whl
